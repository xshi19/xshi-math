# Rules

Canonical constraints for agents and humans. Codex reaches these files
through `AGENTS.md`. Cursor should get thin `.cursor/rules/*.mdc`
adapters that point here — see
[AGENT_FRAMEWORK.md](../design/AGENT_FRAMEWORK.md).

No rule bodies yet. Add them when the first real workflow exists, not
speculatively.

## Planned files (scaffold / migration)

| File | Purpose |
| --- | --- |
| `content.md` | Page types, claim backing, notation, copyright/paraphrase |
| `writing.md` | Prose rigor; no claim-widening style passes |
| `coding.md` | Python / demo conventions; import from packages |
| `agent-guidance.md` | When to edit `AGENTS.md` vs rules vs skills |

Track-specific deltas belong in `tracks/<name>/AGENTS.md`, not as a
second copy of these files.
